import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:slidy_modular_aula_4/app/p_controller.dart';
import 'package:slidy_modular_aula_4/app/app_module.dart';

void main() {
  initModule(AppModule());
  // PController p;
  //
  setUp(() {
    //     p = AppModule.to.get<PController>();
  });

  group('PController Test', () {
    //   test("First Test", () {
    //     expect(p, isInstanceOf<PController>());
    //   });

    //   test("Set Value", () {
    //     expect(p.value, equals(0));
    //     p.increment();
    //     expect(p.value, equals(1));
    //   });
  });
}
