import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:bottomnavigatorbar/app/modules/start/pages/perfil/perfil_controller.dart';
import 'package:bottomnavigatorbar/app/modules/start/start_module.dart';

void main() {
  initModule(StartModule());
  // PerfilController perfil;
  //
  setUp(() {
    //     perfil = StartModule.to.get<PerfilController>();
  });

  group('PerfilController Test', () {
    //   test("First Test", () {
    //     expect(perfil, isInstanceOf<PerfilController>());
    //   });

    //   test("Set Value", () {
    //     expect(perfil.value, equals(0));
    //     perfil.increment();
    //     expect(perfil.value, equals(1));
    //   });
  });
}
